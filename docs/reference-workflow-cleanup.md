# Reference 读取与检查职责

## 行为修正与依据

- **文风按维度覆盖**：#395（`1e2488b`）把文风中的「停读」表转换成整份 reference 跳过指令。现在删除这层转换，保留文风全文输入；文件按任务条件读取，冲突的表达条款按 #412 的优先级裁决。回归测试覆盖具体文件、通配路径和局部判读表，验证它们不会改变组装 prompt，也不会修改文风文件。
- **身体词按功能判断**：`d2cad637`（2026-05-04）引入短篇「身体部位同一词全文 ≤ 5 次」。已有执行记录显示模型用 `s.count()` 把“手机、手续”等计入“手=28”，为过配额将“把手机拿到我面前”改为“把转账页面拿到我面前”，将“先抬手试一下”改为“先抬起来试一下”。改用 main 已有写手判据：按叙事功能判断，不设次数上限。同批的字数、节数与扫描门槛保留。

## 内容位置

| 内容 | 文件 | 加载条件 |
|---|---|---|
| 长篇目录、产物映射、缺失处理、权威顺序 | `story-long-write/references/project-files.md` | 首次定位、创建或缺失 |
| 长短篇参考目录 | 各 skill 的 `references/reference-index.md` | 按阶段或主题定位资料 |
| 长篇写前召回 (a)-(g) | `story-long-write/references/benchmark-recall.md` | 每章写前 |
| 短篇对标发现与召回 | `story-short-write/references/benchmark-recall.md` | 有对标、拆文库或用户参考 |
| 追踪初始化 JSON | `tracking-initialization.md`（长篇/import/review 同源） | 初始化前 |
| 去味 Gate 细则与写手判据 | `story-deslop/references/deslop-gates.md`，部署副本在 `agent-references/` | 实际执行者读取所选 Gate |

以上段落和示例保留原文，只调整位置与路径。独立去味详版和写手判据按原用途分节，共享清单校验部署副本。

## 检查职责

写手负责内容覆盖、编排与格式；质量阶段由一个执行者完成语义去味；主会话对最终文件运行扫描和交付验收。分工传入长短篇写作、审查及手动回落的 prompt。未分配职责的独立调用沿用写手自检。

保留 #403 的所选 Gate 范围、#406 的情绪直写和功能性身体细节、#412 的文风优先级；内部三遍法、对话检查、一致性检查和最终复验仍执行。#395 的八槽组装、分组交付、时空表、新增物三档、字数处置与追踪事务保持原契约。

## 预算与验证

入口缩短来自按需加载，迁出文件仍计入同阶段预算。路由和职责说明的预算增量已在 `scripts/doc-budget.json` 标明：长篇 +200/+900、短篇 +500；agent 路径补计完整 Gate 文件。该口径不代表整个会话的 token 用量。

验证覆盖写手管线、文风优先级、Reference Gate、文件可达性、共享副本、文档预算、扫描器、追踪与多端部署。本文所述收益是读取和检查行为修正，整体成稿质量仍需由实际使用评价。
